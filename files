// app/globals.css
:root {
  --radius: 4px;
  --mobile-nav-height: 56px; /* Added for mobile nav height */
}

@layer utilities {
  .pb-safe-nav {
    padding-bottom: calc(var(--mobile-nav-height) + env(safe-area-inset-bottom, 0px));
  }
  .pb-safe-area {
    padding-bottom: env(safe-area-inset-bottom, 0px);
  }
  /* rest of code here */
}

// app/page.tsx
import { useState, useRef, useEffect } from 'react';
import { useRouter } from 'next/router';

const Portfolio = () => {
  const [activeSection, setActiveSection] = useState('home');
  const sectionRefs = useRef<{ [key: string]: HTMLElement | null }>({});
  const mobileNavRef = useRef<HTMLElement | null>(null); // Added for mobile nav ref

  useEffect(() => {
    const updateNavHeight = () => {
      const h = mobileNavRef.current?.offsetHeight ?? 0;
      document.documentElement.style.setProperty("--mobile-nav-height", `${h}px`);
    };
    updateNavHeight();
    window.addEventListener("resize", updateNavHeight);
    return () => window.removeEventListener("resize", updateNavHeight);
  }, []); // Added for effect to compute and set mobile nav height

  const router = useRouter();

  return (
    <div className="relative">
      <main
        className={`transition-all duration-500 ease-in-out pt-0 sm:pt-8 md:pt-8 pb-safe-nav md:pb-0 ${activeSection === "home" ? "" : "md:pb-4"}`}
      >
        {/* rest of code here */}
      </main>
      <nav ref={mobileNavRef} className="fixed bottom-0 left-0 right-0 z-50 md:hidden">
        <div className="flex items-center justify-around px-2 py-2 gap-2 pb-safe-area">
          {/* rest of code here */}
        </div>
      </nav>
    </div>
  );
};

export default Portfolio;
